class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        return x >=0 and [int(a) for a in str(x)] == [int(a) for a in str(x)][::-1]
        
